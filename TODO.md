# Fire Path - 남은 작업 목록

> 완료된 작업의 상세 구현 기록은 **소스코드가 원본**이므로 이 문서에 남기지 않는다.
> 여기 남기는 것은 ① 아직 안 한 일 ② 소스코드로는 알 수 없는 것(실패한 시도·함정·보류 결정)뿐이다.

---

## 🔴 해야 할 것

### 앱 업데이트 안내 팝업 — 다음 .aab 빌드가 필요하다

코드 작업은 완료(`src/services/appUpdate.ts`, `AppUpdateDialog.vue`, `App.vue`). 자세한 내용은
**CAPACITOR.md 8절** 참고.

네이티브 플러그인(`@capawesome/capacitor-app-update`)을 추가했으므로 **웹 배포만으로는 반영되지
않는다.** `npx cap sync android` 는 이미 돌려서 gradle 파일에 반영해 뒀고, 남은 것은:

```bash
npm install                # 새 플러그인 받기 (빼먹으면 gradle 빌드가 실패한다)
npm run deploy             # 웹 배포 — 팝업 UI·로직은 웹 코드다
npm run build:android      # 타입 체크 + cap sync
# android/app/build.gradle 의 versionCode 를 7 이상으로 올린 뒤
cd android && ./gradlew clean && ./gradlew bundleRelease
```

그다음 테스트 트랙에 올리고, 그 버전을 스토어에서 설치한 뒤 다시 더 높은 버전을 올려서
**팝업이 뜨는지 실기기 확인**.

> 확인은 반드시 스토어로 설치한 빌드에서 해야 한다. In-App Updates API는 로컬 디버그 빌드나
> 사이드로드 설치본에서는 동작하지 않는다(팝업이 안 뜨는 게 정상).
>
> 그리고 **이 기능은 다음 릴리스부터 동작한다.** 지금 깔려 있는 `versionCode 6`에는 이 코드가
> 없어서 7 출시를 안내받지 못한다. 7 이상 사용자부터 적용된다.

### 앱인토스 프로모션 — 조기종료 (2026-08-17 결정)

10원(참여 저조로 08-06 종료)에 이어 50원도 유입이 저조해 **조기종료**하기로 했다.
**소스는 고치지 않는다** — 카드 노출(허브/대시보드/가계부 3곳)과 라우터 가드의 방문 기록이 전부
`isPromotionAvailable()`(= 코드가 비어 있으면 false) 뒤에 있어서, `.env`의
`VITE_TOSS_PROMOTION_CODE`를 비운 번들만 올리면 저절로 꺼진다. 카드·서비스·테이블·RPC는
다음 프로모션 가능성에 대비해 그대로 둔다.

**종료 순서 — 번들 출시가 먼저, 콘솔 종료가 나중.** 카드 노출 조건에 "진행 중" 확인이 없어서
콘솔만 먼저 종료하면 카드가 남은 채 [받기]가 실패한다. 브리지가 에러 코드 없이 `'ERROR'`만
돌려주면 `errorCodeOf`가 null이라 **PENDING인 채로 남고 재시도가 영구히 막힌다**(재시도는
FAILED에서만 허용).

1. [ ] 로컬 `.env`의 `VITE_TOSS_PROMOTION_CODE`를 빈값으로
2. [ ] `rm -rf dist && npm run build:toss` — **`dist`를 안 지우면 예전 번들이 재사용된다**(아래
   함정 절). 업로드 전 `grep -r "<운영 코드>" dist/web/assets/*.js`가 빈 결과인지 확인
3. [ ] `firepath.ait` 콘솔 업로드 → 검수 → 출시 — 출시 즉시 전원이 새 번들을 받아 카드가 사라진다
4. [ ] **출시 직후** 콘솔에서 **[종료하기]** — 남은 예산은 비즈 월렛으로 자동 환급(반영이 늦어질
   수 있음). 혜택 탭 미션 노출도 종료해야 사라지므로 출시와 사이를 벌리지 말 것
5. [ ] PENDING 잔여 확인 — 출시 전에 [받기]를 눌러 걸린 사람이 있으면 아래 SQL로 풀어준다

```sql
UPDATE toss_promotion_rewards
SET status = 'FAILED', error_code = 'MANUAL_RESET', updated_at = now()
WHERE promotion_code = '<운영 코드>' AND status = 'PENDING';
```

> 급하면 콘솔 [종료하기]를 먼저 해도 된다 — 지급 차단은 토스 서버가 강제하므로 돈은 안 샌다.
> 대신 새 번들 출시까지 카드가 계속 떠서 실패·PENDING 이력이 쌓이므로 5번 정리가 필수가 된다.
> 운영 코드는 콘솔 상세에서 확인한다 — `.env`는 gitignore이고 이 레포는 공개라 커밋하지 않는다.

**이력을 지우지 말 것.** 10원·50원 수령 기록(`toss_promotion_rewards`)과 탈퇴자 보관 이력
(`toss_promotion_reward_archive`)은 클레임이 들어오면 참여 기록을 확인해 파트너사가 처리해야
하는 유일한 근거다. 종료 후에도 보존한다. `TEST_` 이력으로 재테스트하려면 **두 테이블 다**
지워야 한다는 점(재수령 차단 백필이 `TEST_` 행도 보관했다)도 그대로다.

### 프로모션 재수령 차단 — 남은 것

차단 자체는 2026-08-10에 적용 완료(`20260810_01_toss_promotion_reward_archive.sql`). 남은 것:

- [ ] **개인정보처리방침에 참여 이력 보관 근거·기간 추가** — 보관 테이블에 탈퇴자의
  `getAnonymousKey()` 해시가 남는다(이메일·이름 없음). "부정 재수령 방지 목적" 근거와
  보관기간을 넣어둘 것. 조기종료(2026-08-17) 뒤에도 클레임 대응용으로 이력을 보존하므로
  "프로모션 종료 시까지"로는 못 쓴다 — 클레임 처리 여지가 끝나는 시점으로 잡을 것
- [ ] (선택) **`admin-delete-user` Edge Function도 이력 보관** — 이 함수는 `auth.admin.deleteUser()`를
  직접 호출해 `delete_user_account` RPC를 거치지 않아, 관리자가 지운 계정 이력은 CASCADE로 사라진다.
  관리자 전용 경로라 일단 두기로 했다(2026-08-10 결정)
- [ ] (선택) **S2S 지급 전환** — 아래 "재시도 금지 · 함정" 절 참고

**실측 피해 (2026-08-10, 콘솔 대조)**

| 프로모션 | 콘솔 소진액 | DB `GRANTED` | 차액 |
| --- | --- | --- | --- |
| 10원 (07-27~08-03) | 70원 (7건) | 70원 (7건) | **0원** |
| 50원 (08-06~08-10) | 500원 (10건) | 250원 (5건) | **250원 (5건)** |

50원은 지급 10건 중 5건이 어뷰징이었다. 진행 기간 5일 × 하루 캡 50원과 정확히 맞아, 한 사람이
매일 탈퇴→재가입한 패턴이다. 10원 때는 차액이 0이라 같은 구멍이 있었어도 밟은 사람이 없었다.

> 이 대조는 반드시 `promotion_code`로 스코프를 걸어야 성립한다. 코드 구분 없이 합계를 내면 옛 10원
> 코드와 `TEST_` 행까지 섞인다.

**남은 한계** (막지 못하는 경로)

- 적용 시점에 **이미 탈퇴 상태**였던 계정은 식별키가 이미 지워져 한 번 더 받는다. 그 다음 탈퇴에서
  보관되어 영구 차단 — 추가 손실은 50원 1회로 끝난다
- `getAnonymousKey()`가 `undefined`/`ERROR`를 주면 `toss_user_key`가 NULL이라 보관·차단이 안 된다.
  이 경우 **탈퇴 없이 계정만 새로 만들어도** 재수령된다 (부분 유니크 인덱스라 원래도 안 걸렸다).
  2026-08-10 기준 NULL 이력 0건
- 재가입 계정에는 카드가 다시 뜨고 [받기]를 눌러야 "이미 참여한 계정"이 나온다. 같은 토스 계정이
  다른 앱 계정으로 시도할 때와 동작이 같아 그대로 뒀다(2026-08-10 결정)

### 이전 작업에서 넘어온 미완료 액션

**다국가 지원 (2026-07-11 코드 완료)** — 상세 설계는 **GLOBALIZATION.md**,
로케일 추가 방법은 **ADD_LOCALE.md** 참고

- [ ] **`20260711_04` 컬럼 DROP SQL 실행** — `asset_type` 컬럼 제거.
  **이 브랜치가 main 배포·안정화된 뒤에 실행할 것** (지금 실행하면 구버전 프론트 insert가 깨진다)
- [ ] Edge Function 4개 Supabase 재배포 (아직이면) — stock-price / etf-info / etf-backtest / etf-dividend
- [ ] 로컬 `npm install` (vue-i18n 신규 의존성)
- [ ] 실제 로그인 후 자산 등록·거래 추가 플로우 및 English 전환 화면 직접 확인

**토스 로그인 (2026-07-27 완료)**

- [ ] **`20260727_02_backfill_signup_log.sql` 실행** — 토스 가입자가 `signup_log`에 안 남던 버그의
  누락분을 `login_log` 최초 로그인 시각으로 채운다. 토스는 가입 = 첫 로그인이라 값이 실제 가입
  시각과 같다. `NOT EXISTS`로 걸러서 **재실행해도 안전**.
  코드 수정은 2026-08-06 운영 빌드에 실려 지금 검수 중이라, **출시 전까지는 신규 토스 가입자가
  계속 누락된다** — 통계를 볼 때 한 번씩 돌릴 것
- [ ] **`20260727_03_admin_get_user_providers_toss.sql` 실행** — 관리자 화면 provider 배지를
  "가입 경로"에서 "쓸 수 있는 로그인 수단"(구글/카카오/토스/비밀번호)으로 개편
- [ ] **웹 배포** — 관리자 화면은 웹에서 보므로 미니앱 재업로드는 불필요.
  `/terms`가 열려야 콘솔 약관 링크도 유효해진다
- [ ] **`toss-login` 브랜치 main 머지**
- [ ] **미확인 경로**: 토스 계정에 이메일이 없는 경우의 이메일 입력 화면 — 재현이 어려워 미검증.
  토스 가입 시 이메일은 필수가 아니라 `null`로 올 수 있다(`login-intro.md:76`)

**성능 (2026-07-28 1차 완료)** — 상세는 **PERFORMANCE.md 7절**. 급하지 않음

- [ ] MDI 폰트 축소 — 7,460개 중 122개만 사용. ~450 kB 절감 가능하나 122곳 수정이라 diff가 크다
- [ ] 시세 배치 조회 — `price_cache` 적용으로 가치가 불확실해졌다. 자산 화면 체감 확인 후 판단

### 🔍 사용자 검증만 남음

- **가계부 다국가 지원 (영어 + 기준통화)**: 2026-07-13 코드 작업 전부 완료, SQL도 실행 완료.
  남은 것은 사용자 단계 5(영어 계정 시딩 검증), 6(엑셀 한/영 양식 검증), 7(최종 통합 검증 후 main 배포).
  상세는 **BUDGET_GLOBALIZATION.md** 참고

---

## 🧭 토스 로그인 구조 (요약)

CLAUDE.md가 이 절을 가리킨다. 구현 상세는 `supabase/functions/toss-login.ts` 주석이 원본이고,
공식 문서 사본은 **`toss-docs/`** 폴더(README에 색인).

```
appLogin()                  → { authorizationCode, referrer }
toss-login Edge Function    → generate-token → login-me (mTLS 필수)
                            → userKey + 이메일(AES-256-GCM 암호화)
admin.createUser({ email, email_confirm: true })   ← 확인 메일 발송 안 함
admin.generateLink({ type:'magiclink', email })    → hashed_token (발송 안 함)
verifyOtp({ token_hash })   → 미니앱에 세션 확립
```

**계정 매칭 순서** (이메일을 준-key로 쓰는 구조라 같은 이메일이면 같은 회원)

1. `toss_identities`에 `userKey` 매핑이 있으면 그 계정
2. 없으면 토스가 준 이메일로 기존 계정 매칭 → 매핑 추가
3. 이메일도 없으면(토스 가입 시 필수 아님) 미니앱 안에서 직접 입력받아 신규 생성

3번에서 유저가 **직접 입력한** 이메일이 기존 계정과 겹치면 **거부**한다 — 소유 확인이 없어 아무
이메일이나 타이핑해 남의 계정을 가로챌 수 있기 때문. 토스가 준 이메일은 토스 계정 등록값이라 이
제한을 두지 않는다. 관리자 이메일은 토스 자동 연결 대상에서 제외한다(회원 삭제·비밀번호 초기화 권한).

**이메일 없이는 가입이 성립하지 않는다.** `generateLink({ email })`에 이메일이 필수인데
supabase-js에 "이 유저로 세션 만들기" API가 없어 우회가 불가능하다. 게다가 `feedback` RLS가
`email = jwt.claims->>'email'` 기준이라 이메일이 null이면 본인 의견이 영원히 안 보이고,
`login_log`·`access_log`의 `email NOT NULL`, `record_signup(user_email)` 집계도 전부 깨진다.

---

## ⛔ 재시도 금지 · 함정 기록

소스코드에는 남지 않는 것들(안 한 것, 실패한 것, 외부 시스템 사실). 지우지 말 것.

**다시 시도하지 말 것**

- **`redirectTo`를 `intoss://firepath/`로 바꿔 딥링크로 세션 복원** (2026-07-26 실패).
  `getSchemeUri()`는 "처음 진입한 스킴 값"이라 페이지 이동이 반영되지 않고, 실기기에서도 실행
  스킴만 나오고 토큰이 없었다. 실행 중 미니앱에 딥링크가 도착했을 때 받을 **이벤트 API가 SDK에 없다**
  (이벤트 브리지는 광고·구매·위치·safeArea뿐). 부작용으로 SNS 로그인이 1탭 → 2탭이 되어 되돌렸다
- **`vite-plugin-vuetify`의 `styles: 'none'`** — `import 'vuetify/styles'`까지 제거한다. 이걸 모르고
  설정했다가 컴포넌트 스타일과 유틸리티 클래스가 전부 사라진 채로 **타입 체크·빌드가 통과했다.**
  기본값(`styles: true`)을 쓸 것. 검증 방법은 PERFORMANCE.md 5절

**빌드 · 배포 함정**

- **`.env`의 `VITE_TOSS_PROMOTION_CODE`는 빌드 시점에 번들에 박힌다.** 코드를 바꿀 때는 반드시
  `rm -rf dist && npm run build:toss` — **`dist`를 안 지우면 `ait build`가 `vite build`를 건너뛰고
  예전 번들을 재사용한다.** 2026-07-27에 실제로 밟았다(테스트 코드 빌드를 검수에 올렸다가 취소).
  업로드 전 `grep -r "TEST_" dist/web/assets/*.js`로 확인하면 확실하다
- **토스 로그인이 콘솔에서 승인되기 전에 그 빌드를 미니앱에 올리면 로그인이 아예 막힌다.**
  미니앱 배포는 수동 업로드라 순서만 지키면 된다
- 미니앱은 서버가 번들을 내려주는 구조라 **출시 즉시 전원이 새 번들을 받는다.** 안드로이드 앱처럼
  구버전 사용자가 남지 않는다

**외부 시스템 사실**

- **`TEST_` 프로모션 지급은 실제 포인트가 차감되지 않아 콘솔 소진액에 잡히지 않는다.** DB에는
  50원짜리 `GRANTED`로 남으므로 콘솔과 대조할 때 반드시 제외할 것 (2026-08-10 10원 프로모션
  콘솔 70원 = DB 70원으로 실증)
- **이메일 가입은 Resend 무료 플랜(월 3,000통·하루 100통)** 이라 프로모션 유입을 감당할 수 없다.
  유료는 $20/월(50,000통). 다만 돈으로 안 풀리는 문제가 따로 있다 — 미니앱 신규 유저가 이메일
  인증을 하려면 `미니앱 이탈 → 메일 확인 → 링크 클릭(웹이 열림) → 미니앱 복귀`를 거쳐야 한다.
  → **미니앱에 마찰 없는 가입 경로가 없으면 프로모션의 신규 유입 효과가 안 난다** (토스 로그인 도입 이유)
- **Supabase 대시보드에서 계정을 직접 지우면 `signup_log`에 활성 회원으로 남는다.**
  `signup_log`·`feedback`은 `user_id`가 없고 이메일 기준이라 CASCADE 대상이 아니다. 앱 내 탈퇴는
  `delete_user_account()`가 처리하지만 대시보드 삭제는 그 RPC가 돌지 않는다.
  ```sql
  DELETE FROM signup_log WHERE email = '<지운 계정 이메일>';
  ```

**알려진 한계 (덮어둔 것)**

- `verifyOtp` 자동 재시도(`tossLogin.ts`의 `signInWithToss`)는 **증상을 덮는 처리**다. 첫 시도에서만
  2탭이 필요했고 이후 재현되지 않았다. 원인을 잡으면 걷어낼 것
- provider 배지: 토스 가입자가 웹에서 비밀번호를 재설정하면 실제로는 비밀번호 로그인이 가능해지는데
  배지에는 안 뜬다. 미니앱은 `tossOnly`라 비밀번호 찾기 버튼이 없어 웹까지 찾아가야 하는 드문 경로

---

## 💭 검토했으나 보류한 것 (나중에 다시 꺼내지 않도록 기록)

**S2S(서버 지급) 방식** — 비게임 미니앱은 서버에서 포인트를 지급할 수 있고 요청 위변조 방지에
유리하다(`promotion.md:44`). **토스 로그인이 전제인데 이미 구현돼 있어 전환 가능하다.** 지금은
클라이언트 브리지 지급이라 우리 DB 차단이 정직한 클라이언트에만 적용되고, 작정한 어뷰징의 마지막
방어선은 콘솔의 1인 하루 캡뿐이다. 위변조가 실제 문제가 되면 그때 전환할 것.

**`getAnonymousKey()`로 로그인 대체 (탈출구)** — mTLS·서버·약관·검수 없이 미니앱 안에서 고유
식별자를 준다. 다만 이메일을 못 받아 합성 이메일을 써야 하고, 그러면 같은 사람이 웹에서 실제
이메일로 가입했을 때 계정이 둘로 갈린다 — 없애려던 중복이 그대로 생긴다. 최후의 수단으로만.

**연도별 예상자산 추이 - 지난 연도 표시** — `AnalysisView.vue`의 이 카드는 `year = currentYear`부터
도는 순수 예측 테이블이라 `past` 상태 스타일이 있어도 실제로는 죽은 코드다. "지난 연도도 보여주자"는
아이디어가 있었으나 **보류**: 과거 자산 기록을 참조하지 않으므로 지난 연도를 "완료 100%"로
하드코딩하면 중간에 매도해 자산이 줄어든 경우에도 "완료"로 표시돼 **잘못된 정보**가 된다.
정확히 하려면 `asset_history` 스냅샷과 "그 시점 목표"를 비교해야 하는데, 목표·월 투자금·기대수익률을
나중에 바꾸면 "그때의 목표"가 무엇이었는지 애매해져 설계가 복잡해진다. 실익 대비 복잡도가 높다고 판단.
