# Fire Path - 남은 작업 목록

## 🔴 해야 할 것

### 앱 업데이트 안내 팝업 — 다음 .aab 빌드가 필요하다

코드 작업은 완료(`src/services/appUpdate.ts`, `AppUpdateDialog.vue`, `App.vue`). 자세한 내용은
**CAPACITOR.md 8절** 참고.

네이티브 플러그인(`@capawesome/capacitor-app-update`)을 추가했으므로 **웹 배포만으로는 반영되지
않는다.** `npx cap sync android` 는 이미 돌려서 gradle 파일에 반영해 뒀고, 남은 것은:

```bash
npm install                # 새 플러그인 받기 (빼먹으면 gradle 빌드가 실패한다)
npm run deploy             # 웹 배포 — 팝업 UI·로직은 웹 코드다
npm run build:android      # 타입 체크 + cap sync
# android/app/build.gradle 의 versionCode 를 7 이상으로 올린 뒤
cd android && ./gradlew clean && ./gradlew bundleRelease
```

그다음 테스트 트랙에 올리고, 그 버전을 스토어에서 설치한 뒤 다시 더 높은 버전을 올려서
**팝업이 뜨는지 실기기 확인**.

> 확인은 반드시 스토어로 설치한 빌드에서 해야 한다. In-App Updates API는 로컬 디버그 빌드나
> 사이드로드 설치본에서는 동작하지 않는다(팝업이 안 뜨는 게 정상).
>
> 그리고 **이 기능은 다음 릴리스부터 동작한다.** 지금 깔려 있는 `versionCode 6`에는 이 코드가
> 없어서 7 출시를 안내받지 못한다. 7 이상 사용자부터 적용된다.

### 앱인토스 프로모션 — 50원으로 재등록

10원으로 진행한 프로모션은 참여가 저조해 **2026-08-06 종료**했다. 조건은 그대로 두고 금액만
**50원**으로 올려 새로 등록했고, 같은 날 승인까지 끝났다.

카드·중복 방지 테이블/RPC는 2026-07-26에 끝나 있고, 이번에 소스에서 바뀐 것은
`src/services/tossPromotion.ts`의 `PROMOTION_AMOUNT` **한 줄뿐**이다(10 → 50). 이 상수 하나가
브리지 호출 `amount`, RPC `p_amount`, 카드 문구 `{amount}` 세 곳으로 흘러간다. 로케일은 보간이라
문구 수정이 필요 없고, `amount`는 RPC 인자로 들어오는 컬럼이라 마이그레이션도 필요 없다.

**지급 조건** (코드 변경 없이 그대로 성립한다):
라우터 가드가 `to.meta.module`이 `asset`/`budget`인 화면에 진입할 때 방문을 기록한다.
`/budget`은 진입 즉시 충족, `/dashboard`는 `requiresGoal` 가드 때문에 **목표자산을 설정해야**
도달한다 → 실질적으로 "가계부 진입 또는 투자 목표 설정"이다. `/goalSettings`에는 `module` 메타가
없어 목표 설정 화면 자체로는 충족되지 않는다.

**콘솔 등록 내용** (2026-08-06 승인 완료, `진행 전` 상태)

| 항목 | 값 |
| --- | --- |
| 프로모션 이름 | 회원가입 후 서비스 카테고리 둘러보면 50원 지급 |
| 혜택탭 미션 이름 | 서비스 이용하기 |
| 지급 금액 | 50원 (1인 1회 — 앱이 계정당 평생 1회로 막는다) |
| 1인 하루 최대 지급 금액 | 50원 (지난번 10원 → 안 올렸으면 두 번째 참여자부터 지급이 막혔다) |
| 전체 예산 | 99,900원 |
| 이동 URL | `intoss://firepath` — 미로그인 신규 유저는 로그인 화면 → 가입 → 허브로 흐르므로 `/hub`를 붙일 필요가 없다 |

지급 금액은 콘솔 "수정"으로 못 바꾼다(수정은 사실상 예산 증액용). 새 프로모션을 등록해야 하고,
그러면 **프로모션 코드도 새로 발급된다.** 코드는 콘솔 프로모션 상세에서 확인한다 — `.env`는
gitignore이고 이 레포는 공개라 코드를 커밋하지 않는다.

**남은 순서** (2026-08-06 기준 4번 검수 진행 중)

1. ~~콘솔에서 50원 프로모션 등록 → [검토 요청하기]~~ (2026-08-06 승인 완료)
2. ~~**테스트 빌드** — `.env`에 `TEST_` 코드를 넣고 빌드 → 콘솔 QR로 열어 지급 API 1회 테스트~~
   (2026-08-06 통과. 이 빌드를 검수에 올리면 안 된다 — 2026-07-27에 실제로 올렸다가 취소했다)
3. ~~**운영 빌드** — `.env`를 운영 코드로 바꾸고 `rm -rf dist && npm run build:toss`,
   `grep -r "TEST_" dist/web/assets/*.js` 로 테스트 코드가 안 섞였는지 확인~~ (2026-08-06 완료)
4. `firepath.ait` 콘솔 업로드 → **검수 진행 중** → 출시
5. **출시되면 곧바로** 콘솔에서 **[프로모션 시작하기]** — 아래 경고 참고
6. 테스트 이력 정리 — 아래 `DELETE ... LIKE 'TEST_%'`

> 3번 재빌드에는 signup_log 수정도 함께 실려 나간다(출시본 `20260727-11`에 빠져 있던 것).
> 미니앱은 서버가 번들을 내려주는 구조라 출시 즉시 전원이 새 번들을 받는다. 안드로이드 앱처럼
> 구버전 사용자가 남지 않는다.

> ⚠️ **출시와 [시작하기] 사이를 벌리지 말 것.** 카드 노출 조건은 `PROMOTION_CODE !== '' &&
> isTossApp()` 뿐이라 프로모션이 `진행 전`이어도 카드가 뜬다. 그 사이에 [받기]를 누르면
> `claim_toss_promotion`이 PENDING 행을 먼저 만들고 지급이 실패하는데, 브리지가 에러 코드 없이
> `'ERROR'`만 돌려주면 `errorCodeOf`가 null이라 **PENDING인 채로 남고 재시도가 영구히 막힌다**
> (재시도는 FAILED에서만 허용). 걸린 사람이 생기면 아래 SQL로 풀어준다.
>
> ```sql
> UPDATE toss_promotion_rewards
> SET status = 'FAILED', error_code = 'MANUAL_RESET', updated_at = now()
> WHERE promotion_code = '<운영 코드>' AND status = 'PENDING';
> ```

**기존 10원 수령자도 50원을 다시 받는다.** 중복 차단 유니크 인덱스가 `(user_id, promotion_code)`
/ `(toss_user_key, promotion_code)`라서 코드가 바뀌면 완전히 별개 이력이고, `fetchRewardStatus`도
`promotion_code`로 필터하니 지난번 받은 계정에도 카드가 다시 열린다. 새 프로모션이니 그대로
두기로 했다(2026-08-06 결정). 막으려면 RPC가 코드와 무관하게 이전 GRANTED 이력을 보도록 고쳐야 한다.

**DB는 손댈 게 없다.** 이 테이블을 읽는 코드는 `tossPromotion.ts` 하나뿐이고(관리자 화면·집계
뷰·cron 어디서도 안 읽는다), 조회·INSERT·UPDATE 세 경로가 전부 `promotion_code`로 스코프돼
있어서 옛 코드 행은 읽히지도 갱신되지도 않는다. 마이그레이션도 데이터 정리도 필요 없다.
옛 코드의 PENDING 행은 영구히 PENDING으로 남지만(자기 코드 행만 전이시키므로) 유저 영향은 없다.

**옛 이력을 지우지 말 것.** 새 프로모션 동작에는 영향이 없지만 누가 10원을 받았는지의 유일한
기록이고, 클레임이 들어오면 참여 기록을 확인해 파트너사가 처리해야 한다. 지울 대상은 `TEST_` 행뿐이다.

"둘러보기" 달성 기록(`fp-toss-promo-visited:{userId}` localStorage)도 프로모션 코드가 안 붙어
있어, 지난번 둘러본 계정은 허브에서 바로 `ready`로 뜬다. 조건이 같고 어차피 카테고리 진입 시
가드가 다시 기록하므로 그대로 둔다.

**함정 기록 (2026-07-27에 실제로 밟았다)**: `.env`의 `VITE_TOSS_PROMOTION_CODE`는 **빌드 시점에
번들에 박힌다.** 테스트 코드(`TEST_` 접두어)로 빌드한 `20260727-10`을 그대로 검수에 올렸다가
요청 취소하고, 운영 코드로 다시 빌드해 `20260727-11`로 재등록했다. 코드를 바꿀 때는 반드시
`rm -rf dist && npm run build:toss` — `dist`를 안 지우면 `ait build`가 `vite build`를 건너뛰고
예전 번들을 재사용한다. 업로드 전 `grep -r "TEST_" dist/web/assets/*.js`로 확인하면 확실하다.

테스트 이력을 지우고 다시 받아보려면(같은 계정으로 재수령 테스트):

```sql
DELETE FROM toss_promotion_rewards WHERE promotion_code LIKE 'TEST_%';
```

운영 코드와 `TEST_` 코드는 `promotion_code`가 달라 중복 차단이 서로 간섭하지 않는다. 남겨둬도
동작에는 문제없고, 콘솔 소진율과 DB 이력을 비교할 때 헷갈리지 않도록 지우는 것뿐이다.

### 앱인토스 프로모션 — 탈퇴 후 재가입 재수령 차단 (2026-08-10)

**증상**: 탈퇴하고 다시 가입하면 프로모션을 몇 번이든 다시 받을 수 있었다. 예산 99,900원 ÷ 50원
= 1,998회라 한 사람이 반복하면 전체 예산을 소진할 수 있다.

**원인**: `toss_promotion_rewards.user_id`가 `auth.users`를 `ON DELETE CASCADE`로 참조한다.
탈퇴(`delete_user_account`)가 `auth.users` 행을 지우면 지급 이력이 통째로 사라지는데, 여기엔
중복 차단의 근거인 `toss_user_key`도 들어 있다. 재가입하면 `user_id`가 새로 발급되므로 유니크
인덱스 두 개가 **모두** 무력화된다. 특히 `(toss_user_key, promotion_code)` 인덱스는 "같은 토스
계정이 앱 계정 여러 개로 중복 수령"을 막으려고 만든 건데, 정작 가장 쉬운 우회로인 탈퇴 후
재가입에는 근거 데이터가 함께 지워져서 작동하지 않았다.

마찰도 없다 — 탈퇴 시 `disconnectToss()`가 토스 연결까지 끊어줘서 재로그인 흐름이 깔끔하고,
`fp-toss-promo-visited` localStorage는 `/budget` 진입만으로 라우터 가드가 다시 기록한다.

**해결**: `toss_promotion_reward_archive` 테이블(FK 없음)을 두고, `delete_user_account`가 계정
삭제 직전에 이력을 옮긴다. `claim_toss_promotion`은 예약 전에 이 테이블을 확인해 `'ALREADY'`를
반환한다. `getAnonymousKey()` 해시는 앱 계정 삭제와 무관하게 같은 값이라 재수령 판정의 안정적인
기준이 된다(`toss-docs/user-hash-key-develop.md` 27행). `signup_log`가 이미 쓰는 패턴과 같은 결이다.
스코프는 현행 그대로 **`promotion_code`별 1회** — 위 88~91행 결정(10원 수령자도 50원을 다시 받는다)을
유지한다.

**DB만 바뀐다.** `tossPromotion.ts`는 손대지 않았고 RPC 시그니처·반환값도 그대로라 미니앱
재빌드·재업로드가 필요 없다. 검수 진행 상태와 무관하게 마이그레이션만 먼저 적용하면 된다.
**[프로모션 시작하기] 전에 적용해두는 게 좋다.**

**한계** (알고 넘어가는 것):

- 이 마이그레이션 이전에 이미 탈퇴한 계정의 이력은 복구할 수 없다. 그때 사라진 행은 DB에 흔적이
  없어서 과거 재수령을 조회로 잡아낼 방법도 없다. 콘솔 소진액과
  `SELECT sum(amount) FROM toss_promotion_rewards WHERE status = 'GRANTED'` 차액으로 추정만 된다.
- `admin-delete-user` Edge Function은 `auth.admin.deleteUser()`를 직접 호출해 이 RPC를 거치지
  않으므로, 관리자가 지운 계정의 이력은 지금도 CASCADE로 사라진다. 관리자만 쓸 수 있는 경로라
  일단 두었다.
- `getAnonymousKey()`가 SDK 2.4.5 미만에서 `undefined`를 반환하면 `toss_user_key`가 NULL이라
  보관 대상에서 빠진다. 그 기기에서는 **탈퇴 없이 계정만 새로 만들어도** 중복 수령이 된다
  (유니크 인덱스가 `WHERE toss_user_key IS NOT NULL` 부분 인덱스라 원래도 안 걸렸다).
- 재가입한 계정에는 카드가 다시 뜨고 [받기]를 눌러야 "이미 참여한 계정"이 나온다.
  `fetchRewardStatus`는 살아있는 이력만 보기 때문인데, 같은 토스 계정이 다른 앱 계정으로
  중복 수령을 시도할 때와 동작이 같아서 그대로 뒀다. 카드를 미리 숨기려면 보관 이력을
  조회하는 RPC가 하나 더 필요하다.

**개인정보**: 보관 테이블에는 `getAnonymousKey()` 해시·프로모션 코드·금액·상태·리워드 키만
남는다(이메일·이름 없음). 그래도 탈퇴자의 식별키를 계속 갖고 있는 것이므로, 개인정보처리방침에
**부정 재수령 방지 목적의 참여 이력 보관 근거와 보관기간**(예: 프로모션 종료 시까지)을 넣어둘 것.

### ✅ 토스 미니앱 로그인 — 토스 로그인 도입으로 해결 (2026-07-27)

**증상**: 미니앱에서 구글·카카오 로그인을 하면 겉으로는 로그인되지만 세션이 미니앱에 남지 않는다.
프로모션 카드도 안 보이고, 앱을 다시 열면 로그아웃 상태다. 이메일+비밀번호 로그인은 정상.

**원인**: 미니앱 웹뷰 origin은 Supabase Redirect URLs에 없어서 `redirectTo`가 거부되고 Site URL
(`firepath.me`)로 떨어진다. 게다가 토스는 OAuth를 **별도 인앱 브라우저**로 띄우므로, 인증이 끝나면
유저는 미니앱이 아니라 **웹 버전에 로그인된 상태**로 남는다. UI가 같아서 알아채기 어렵고,
토스 브리지가 없어 프로모션 카드만 사라진다.

**시도했으나 실패 (2026-07-26) — 다시 시도하지 말 것**:
`redirectTo`를 `intoss://firepath/`로 바꿔 딥링크로 세션을 복원하려 했으나 불가능했다.
- `getSchemeUri()`는 문서상 **"처음에 화면에 진입한 스킴 값"** 이고 페이지 이동은 반영되지 않는다.
  실기기에서 확인한 값도 실행 스킴(`intoss://firepath?_deploymentId=...`)뿐이고 토큰이 없었다.
- 실행 중인 미니앱에 딥링크가 도착했을 때 받을 **이벤트 API가 SDK에 없다**
  (이벤트 브리지는 광고·구매·위치·safeArea뿐).
- 부작용으로 SNS 로그인이 1탭 → 2탭이 되어 되돌렸다. `isTossApp()`만 `tossApp.ts`에 남겼다.

**왜 프로모션에 치명적인가 (2026-07-26 확인)**: 이메일 가입은 Resend **무료 플랜(월 3,000통,
하루 100통 한도)** 을 쓰고 있어서 프로모션 유입을 감당할 수 없다. 유료 전환은 $20/월(50,000통)로
예산 대비 사소하지만, **돈으로 풀리지 않는 문제가 따로 있다** — 미니앱 신규 유저가 이메일 인증을
하려면 `미니앱 이탈 → 메일 확인 → 링크 클릭(웹이 열림) → 미니앱 복귀`를 거쳐야 한다.
10원 받으려고 이걸 할 사람은 거의 없다(`toss-docs/promotion-intro.md` 265~266행이 경고하는 이탈 지점).
→ **결론: 미니앱에 마찰 없는 신규 가입 경로가 없으면 프로모션의 신규 유입 효과가 안 난다.**

**해결책: 토스 로그인(`appLogin`) 도입.** 2026-07-27 구현·실기기 검증 완료.
이메일을 **한 통도 보내지 않으므로** Resend 한도와 무관해지고, 원탭이라 전환율도 최대가 된다.
공식 문서 사본은 **`toss-docs/`** 폴더에 있다(README에 색인). 아래 행 번호는 그 파일들 기준.

```
appLogin()                  → { authorizationCode, referrer }
toss-login Edge Function    → generate-token → login-me (mTLS 필수)
                            → userKey + 이메일(AES-256-GCM 암호화)
admin.createUser({ email, email_confirm: true })   ← 확인 메일 발송 안 함
admin.generateLink({ type:'magiclink', email })    → hashed_token (발송 안 함)
verifyOtp({ token_hash })   → 미니앱에 세션 확립
```

**계정 매칭 순서** (중복 계정 방지 — 이메일을 준-key로 쓰는 구조라 같은 이메일이면 같은 회원):
1. `toss_identities`에 `userKey` 매핑이 있으면 그 계정
2. 없으면 토스가 준 이메일로 기존 계정 매칭 → 매핑 추가
3. 이메일도 없으면(토스 가입 시 필수 항목이 아님) 미니앱 안에서 직접 입력받아 신규 생성

3번에서 유저가 **직접 입력한** 이메일이 기존 계정과 겹치면 **거부**한다. 소유 확인이 전혀 없어
아무 이메일이나 타이핑해 남의 계정을 가로챌 수 있기 때문. 토스가 준 이메일은 토스 계정 등록값이라
이 제한을 두지 않는다(단 문서상 "점유 인증은 하지 않은 값" — `login-develop.md:250`).
관리자 이메일은 토스 자동 연결 대상에서 제외한다(회원 삭제·비밀번호 초기화 권한 때문).

**이메일이 꼭 필요한 이유 (2026-07-27 재확인)**: 핵심 데이터(포트폴리오·거래·목표·가계부)는 전부
`user_id` 기준이라 멀쩡하지만, 아래는 이메일이 없으면 깨진다.
- **세션 발급 자체** — `generateLink({ email })`에 이메일 필수. supabase-js에 "이 유저로 세션
  만들기" API가 없어서 우회 불가. 즉 "이메일 없이 가입"은 현실적으로 "가짜 이메일로 가입"이 된다
- 본인 의견이 **영원히 안 보임** — RLS가 `email = jwt.claims->>'email'`(`feedback_v2.sql:21,28,31`),
  이메일이 null이면 NULL 비교라 항상 거짓. 호출부 `FeedbackView.vue:88,100`,
  `AssetLayout.vue:86`, `HubView.vue:143`
- `login_log`·`access_log`의 `email NOT NULL` → 빈 문자열로 쌓여 관리자 화면에서 식별 불가
- `record_signup(user_email)`이 호출되지 않아 가입 이력·잔존율 집계에서 누락(`App.vue:34`)

**미니앱에서는 토스 로그인만 쓸 수 있다** (`login-intro.md:22`, 정책). 그래서 `isTossApp()`이면
이메일·구글·카카오 버튼을 전부 숨긴다. 되돌리려면 `LoginView.vue`의 `tossOnly` 하나만 지우면 된다.
⚠️ **토스 로그인이 콘솔에서 승인되기 전에 이 빌드를 미니앱에 올리면 로그인이 아예 막힌다.**
미니앱 배포는 수동 업로드라 순서만 지키면 된다.

**콘솔 · 배포 (2026-07-27 완료)**
1. ~~SQL 실행: `supabase/migrations/20260727_01_toss_identities.sql`~~
2. ~~콘솔 > 토스 로그인 동의 항목~~ — **이메일 + 이름**. 이름은 콘솔에서 필수로 고정돼 해제할 수 없다.
   이름·이메일·성별 범위 안이라 **연결 끊기 콜백은 불필요**하다(`login-intro.md:70`).
   앱은 이름을 읽지도 저장하지도 않지만, 동의는 받으므로 개인정보처리방침 수집 항목에 한 줄 넣어뒀다
3. ~~콘솔 > 약관 등록~~ — 서비스 이용약관 `https://firepath.me/terms`,
   개인정보 수집·이용 동의 `https://firepath.me/privacy-policy`, 마케팅 수신 동의(선택).
   동의문의 **만 14세 미만 사용 여부는 '사용 안함'** — 법정대리인 동의 절차가 앱에 없다
4. ~~콘솔 > mTLS 인증서 발급~~ → cert/key PEM
5. ~~콘솔 > '이메일로 복호화 키 받기'~~ — **AAD는 오지 않는다.** 문서 PHP 예제의 `TOSS`가 맞았다
6. ~~Supabase 시크릿 등록~~ — `TOSS_MTLS_CERT`, `TOSS_MTLS_KEY`, `TOSS_DECRYPT_KEY` 3개.
   `TOSS_DECRYPT_AAD`는 등록 불필요(코드 기본값 `TOSS`). 다른 값을 안내받으면 이 시크릿으로 덮어쓴다
7. ~~Edge Function 배포~~ — `toss-login`, `toss-disconnect`
8. ~~앱 검수 요청~~ — `20260727-11` **출시 완료(2026-07-28)**. 검수는 하루 걸렸다

> 검토 체크리스트 문서를 찾아봤으나 `/checklist/login.md`는 404다. 구현에 필요한 정보는
> `toss-docs/`의 문서로 충분해서 더 찾지 않았다.

**아직 남은 것**
- **웹 배포** — `/terms`가 열려야 콘솔 약관 링크가 유효하다
- **`toss-login` 브랜치 main 머지**

**토스 가입자가 관리자 가입 이력에 안 보이던 버그 (2026-07-27 발견 · 코드 수정 완료)**

토스 로그인만 `record_signup`을 부르지 않아 `signup_log`에 기록이 안 남았다. 이메일 가입은
`signUp()`에서, 구글·카카오는 `App.vue`의 `onAuthStateChange`에서 남기는데, 토스는 둘 다 아니다 —
`App.vue`의 핸들러가 `OAUTH_LOGIN_PENDING_KEY` 표식이 있을 때만 동작하는데 그 표식은
리다이렉트 방식인 구글·카카오만 심고, 토스는 `verifyOtp`로 같은 페이지에서 세션을 세우기 때문.
`auth.users`·`toss_identities`·`login_log`에는 정상적으로 남아 있었고 `signup_log`만 비어서,
가입 이력·오늘 가입자 수·잔존율 통계·탈퇴자 집계가 전부 토스 가입자를 누락했다.
→ 세션이 서는 공통 지점인 `LoginView.vue`의 `afterLogin()`에서 호출하도록 수정.

**출시본 `20260727-11`에는 이 수정이 없다.** 미니앱은 번들을 통째로 올리는 구조라 웹 배포로는
안 고쳐지고, `ait build` 재업로드 + 재검수가 필요하다. 그때까지 신규 토스 가입자는 계속 누락된다.

- [ ] **SQL 실행: `supabase/migrations/20260727_02_backfill_signup_log.sql`** — `login_log`의 최초
  로그인 시각으로 누락분을 채운다. 토스는 가입 = 첫 로그인이라 값이 실제 가입 시각과 같다.
  `NOT EXISTS`로 걸러서 **재실행해도 안전**하니, 수정 빌드가 배포될 때까지 통계 볼 때 한 번씩 돌리면 된다
- [ ] **미니앱 재업로드** — `rm -rf dist && npm run build:toss` (위 프로모션 절의 `dist` 함정 참고).
  이번엔 `dist`가 안 지워지면 프로모션 코드는 멀쩡한데 이 수정만 빠진 번들이 올라가니 더 헷갈린다

**관리자 화면 provider 배지 개편 — 구글 / 카카오 / 토스 / 비밀번호 (2026-07-28)**

배지의 의미를 "가입 경로"에서 **"쓸 수 있는 로그인 수단"** 으로 바꿨다. `email` 배지가
`password`로 바뀌고 `toss`가 추가된다. **기존 토스 가입자도 매핑이 이미 있어 백필 없이 바로 보인다.**

토스 계정은 Edge Function이 `admin.createUser`로 만들어 auth 입장에서는 평범한 이메일 계정이다.
2026-07-28 실측으로 확인한 것:

| 가입 경로 | `auth.identities` | `encrypted_password` |
| --- | --- | --- |
| 이메일 | `email` | 있음 |
| 구글/카카오 | `google` / `kakao` | **비어 있음** (OAuth는 email identity 자체가 없다) |
| 토스 | `email` | 있음 — 랜덤값으로 보이고 빈 문자열의 해시도 아니다 |

즉 **토스와 이메일 가입자는 auth 데이터만으로 구분이 불가능하다.** 유일한 구분점이
`toss_identities`이고, "토스가 만든 계정"인지는 Edge Function이 계정 생성과 매핑 저장을 같은
요청에서 처리한다는 점을 이용해 두 `created_at` 간격으로 판정한다 (실측 0.2초 vs 31일).

> ⚠️ 한계: 토스 가입자가 웹에서 비밀번호를 재설정하면 실제로는 비밀번호 로그인이 가능해지는데
> 배지에는 안 뜬다. 미니앱은 `tossOnly`라 비밀번호 찾기 버튼이 없어 웹까지 찾아가야 하는
> 드문 경로라 그대로 뒀다.

- [ ] **SQL 실행: `supabase/migrations/20260727_03_admin_get_user_providers_toss.sql`**
- [ ] **웹 배포** — 관리자 화면은 웹에서 보므로 미니앱 재업로드는 필요 없다

**2026-07-27 실기기 테스트 결과 — 로그인 경로 검증 완료**
- ✅ **`Deno.createHttpClient`(mTLS)가 Supabase Edge Runtime에서 정상 동작한다.** 최대 리스크 해소.
  Cloudflare Worker로 옮길 필요 없다
- ✅ **AAD는 `TOSS`가 맞다.** `TOSS_DECRYPT_AAD` 시크릿 없이 이메일 복호화가 성공한다
- ✅ **신규 가입** — `세션 발급 완료 {"isExisting":false,"hadIdentity":false}`
- ✅ **재로그인** — `세션 발급 완료 {"isExisting":true,"hadIdentity":true}`.
  `toss_identities` 매핑 저장·조회가 정상 동작한다
- ✅ scope 확인 — `login-me 성공 {"scope":"user_email,user_name","hasEmail":true}`
- 첫 시도(14:43)에서 1탭이 아닌 2탭이 필요했으나 **이후 3번의 로그인에서 재현되지 않았다.**
  Invocations 로그상 호출이 1건씩만 찍혀 자동 재시도가 돈 것도 아니다(재시도가 돌면 2건).
  일시적 현상으로 보이나, 신규 가입은 모든 유저가 지나는 경로라 재시도는 안전망으로 남겨둔다
  (`tossLogin.ts`의 `signInWithToss` — 세션 확립 실패에만 1회, 평소엔 돌지 않는다)

**2026-07-27 실기기 테스트에서 발견해 수정한 것 2건**
1. **신규 가입인데 허브가 아니라 가계부로 진입** — `fp-last-module`이 모듈 이름만 저장하는
   **기기 단위(localStorage)** 기록이라, 그 기기에서 전에 가계부를 썼으면 계정이 새로 생겨도
   그 기록이 그대로 적용됐다. 계정을 갈아타는 경우도 같은 문제였다(이메일·구글·카카오 동일).
   → **기록을 계정 단위로 바꿨다** — `{ userId, module }`로 저장하고, 저장된 사용자와 지금
   로그인한 사용자가 다르면 기록이 없는 것으로 본다(→ 허브). 신규 가입은 저장된 기록 자체가
   없으므로 자연히 허브로 간다.
   `afterEach`에는 세션이 없어서 `beforeEach`가 조회한 사용자 id를 모듈 변수로 넘겨 쓴다.
   옛 형식(모듈 이름 문자열)은 누구 것인지 알 수 없어 버린다 — **기존 사용자는 다음 로그인 때
   한 번 허브로 간다.**
2. **토스 계정은 앱 내 회원탈퇴가 불가능했다** — 탈퇴 확인이 `identities`에 `email` provider가
   있으면 비밀번호 재인증을 요구하는 구조인데(`HubView.vue`), 토스 계정은 서버에서
   `admin.createUser`로 만들어져 **email identity는 붙지만 비밀번호가 없다.** 그래서 비밀번호
   계정으로 판정돼 영영 탈퇴할 수 없었다. → `toss_identities` 행이 있으면 비밀번호 없는 계정으로
   보고 **계정 이메일 입력**으로 확인한다.
   소셜(구글·카카오) 전용 계정도 같은 방식으로 통일했다 — 기존엔 `'삭제'` 입력이었는데,
   비밀번호 없는 계정마다 확인 방식이 다른 게 이상하고 이메일 입력이 계정 확인 효과도 있다.

**✅ 회원탈퇴 + 토스 연결 해제도 실기기에서 확인 완료.** `remove-by-user-key`에 Bearer 토큰이
필요한지 문서가 모호했는데(포맷에는 있고 예시에는 없음), **mTLS만으로 호출해도 정상 동작한다.**

**아직 돌려보지 않은 경로**
- **토스 계정에 이메일이 없는 경우**의 이메일 입력 화면 — 재현이 어려워 미확인.
  토스 가입 시 이메일이 필수가 아니라 `null`로 올 수 있다(`login-intro.md:76`).

**알려진 한계**
- `verifyOtp` 자동 재시도(`tossLogin.ts`의 `signInWithToss`)는 **증상을 덮는 처리**다.
  첫 시도(14:43)에서만 2탭이 필요했고 이후 3번의 로그인에서 재현되지 않았다. 원인을 잡으면 걷어낼 것

**그 외 2026-07-27 작업**
- **서비스 이용약관 페이지 신설** (`/terms`) — 콘솔 약관 등록에 넣을 URL이 없었다. 개인정보처리방침과
  같은 구조(`TermsView.vue` + `TermsContentKo/En.vue`)로 맞췄고 로그인 화면 하단에 링크를 나란히 뒀다.
  **투자 정보 면책(제6조)이 핵심.** 표준 문안 기반 초안이라 법률 검토는 사용자 몫
- **개인정보처리방침에 소셜 로그인 제공 항목 추가** — 토스 동의 화면에서 이름 제공에 동의를 받는데
  수집 항목에 이름이 없어 어긋났다. 구글·카카오도 Supabase가 프로필을 `raw_user_meta_data`에
  저장하므로 같은 문제였다. 시행일자 2026-07-27로 갱신
- **이메일 미등록 계정에 탈퇴 경로 추가** (`CompleteEmailView`) — 이메일이 없으면 라우터 가드가
  이 화면 밖으로 못 나가게 막아서 앱 안에서 계정을 지울 방법이 아예 없었다(관리자 요청만 가능)
- **미니앱에서 "홈 화면에 추가" 배너 숨김** — 미니앱은 홈 화면 추가가 불가능하다

**운영 메모 — 대시보드에서 계정을 직접 지울 때**
`signup_log`와 `feedback`은 `user_id`가 없고 **이메일 기준**이라 CASCADE 대상이 아니다.
앱 내 탈퇴는 `delete_user_account()`가 `signup_log.deleted_at`을 먼저 기록하지만,
Supabase 대시보드에서 직접 지우면 그 RPC가 돌지 않아 **가입 이력에 활성 회원으로 남는다.**

```sql
DELETE FROM signup_log WHERE email = '<지운 계정 이메일>';
```

**탈출구 (일정이 위험해지면)**: `getAnonymousKey()`는 **mTLS·서버·약관·검수 전부 없이** 미니앱
안에서 고유 식별자를 준다(`user-hash-key-develop.md:8`, 이미 `tossPromotion.ts`에서 사용 중).
다만 이메일을 못 받아 합성 이메일을 써야 하고, 그러면 같은 사람이 웹에서 실제 이메일로 가입했을 때
계정이 둘로 갈린다 — 없애려던 중복이 그대로 생긴다. 최후의 수단으로만.

**서버 지급(Server-to-Server) 방식은 채택하지 않았다.** 비게임 미니앱은 서버에서 포인트를 지급하는
방식도 쓸 수 있지만(요청 위변조 방지에 유리), **토스 로그인 구현이 전제**다. 이 앱은 Supabase Auth
(이메일·구글·카카오)를 쓰므로 토스 로그인을 새로 붙여야 해서 현재는 클라이언트 브리지 방식으로 구현했다.
위변조 위험이 문제가 되면 그때 토스 로그인 + 서버 지급으로 전환을 검토할 것.

### 🔍 사용자 검증만 남음

- **가계부 다국가 지원 (영어 + 기준통화)**: 2026-07-13 Claude 단계 1~8 **코드 작업 전부 완료**
  (통화 컬럼·저장·집계 환산 + 전 화면 i18n + 시딩 분기 + 엑셀 이중언어). SQL(사용자 단계 1)도 실행 완료.
  남은 것: 사용자 단계 5(영어 계정 시딩 검증), 6(엑셀 한/영 양식 검증), 7(최종 통합 검증 후 main 배포).
  상세 기록은 **BUDGET_GLOBALIZATION.md** 진행 상태 참고.

---

## ✅ 완료된 작업 (2026-07-28) — 성능 개선 1차

상세 분석·측정치·함정은 **PERFORMANCE.md** 참고 (성능 작업 이어서 할 때 먼저 읽을 것).

- **라우터 코드 스플리팅** (`src/router/index.ts`, `budget.routes.ts`) — 뷰 43개를 전부 정적 import
  하던 것을 라우트 단위 동적 import로 전환. 공개 진입점 `LoginView`만 정적 유지.
- **`price_cache` 서버 캐시** (`supabase/functions/stock-price.ts`, 마이그레이션 `20260728_01`) —
  종목마다 Yahoo/Finnhub를 라이브로 때리던 것에 전역 캐시 테이블(TTL 60초)을 앞에 붙였다.
  캐시 실패 시 그대로 외부 API로 폴백하므로 테이블 없이 함수만 배포돼도 안 깨진다.
  **트레이드오프**: 수동 새로고침이라도 60초 내 재조회는 캐시값을 받는다.
- **Vuetify 트리셰이킹** (`vite.config.ts`, `src/plugins/vuetify.ts`) — `import * as components`로
  전 컴포넌트를 전역 등록하던 것을 `vite-plugin-vuetify`의 `autoImport`로 교체. 실사용 34종만 포함.
- **대시보드 논블로킹** (`DashboardView.vue`) — `recomputeAssetSummary`를 `await`한 뒤 렌더하던 것을,
  저장된 값으로 먼저 그리고 재계산은 백그라운드로 돌린 뒤 현재자산만 갱신하도록 변경.
  재계산 중에는 금액 자리에 플레이스홀더를 둔다(지난번 시세 기준 값을 그대로 노출하지 않기 위해).

**측정**: 초기 전송량(gzip) 655 kB → 308 kB (53% 감소).

**⚠️ `vite-plugin-vuetify`의 `styles: 'none'`은 `import 'vuetify/styles'`까지 제거한다.** 이걸 모르고
설정했다가 컴포넌트 스타일과 유틸리티 클래스가 전부 사라진 채로 타입 체크·빌드가 통과했다.
기본값(`styles: true`)을 쓸 것. 자세한 검증 방법은 PERFORMANCE.md 5절.

**남은 성능 작업** (급하지 않음, PERFORMANCE.md 7절)
1. MDI 폰트 축소 — 7,460개 중 122개만 사용. ~450 kB 절감 가능하나 122곳 수정이라 diff가 크다
2. 시세 배치 조회 — `price_cache` 적용으로 가치가 불확실해졌다. 자산 화면 체감 확인 후 판단할 것

---

## ✅ 완료된 작업 (2026-07-13) — 안드로이드 앱 로그인 풀림 수정

- **앱 재실행 시 로그인 화면이 뜨던 문제 수정** (`src/router/index.ts`) — 안드로이드 .aab 업데이트에서
  TWA Start URL을 `/dashboard` → `/`로 바꾼 뒤, `/`(로그인 화면)에 세션 확인·리다이렉트가 없어서 앱을
  열 때마다 로그인 폼이 떠 로그아웃된 것처럼 보이던 문제(세션 자체는 localStorage에 살아 있었음).
  가드에 "`/` 진입 + 세션 존재 → 마지막 사용 모듈(budget→/budget, asset→/dashboard, 없으면 /hub)"
  분기 추가 — LoginView 로그인 성공 후 이동 규칙과 동일. 로그아웃은 전부 signOut 완료 후 `/` 이동이라
  충돌 없음. **웹 배포만 하면 앱 재출시 없이 해결됨** (TWA는 웹을 실시간 로딩). Start URL은 `/` 유지가
  오히려 더 안전한 구조가 됨.

## ✅ 완료된 작업 (2026-07-13) — 전체 점검 및 결함 수정

전체 코드 점검(사용자 관점 문제 요소 체크) 결과 발견된 결함 수정:

- **매도 검증 구멍 2건 수정** (`TransactionAddDialog.vue`)
  - 거래 수정에서 매수→매도로 유형을 바꾸면 원래 매수분이 보유수량에 포함된 채 검증되어 초과 매도가
    통과되던 문제 — `availableForSell`이 수정 대상 거래의 기존 효과(BUY는 빼고 SELL은 더함)를 되돌려 계산하도록 수정
  - "새 종목 추가"로 첫 거래를 매도로 저장하면 보유수량이 음수가 되던 문제 — 새 종목은 매도 가능 수량 0으로
    검증하고, `canSave` 새 종목 분기에도 초과 매도 검증 추가
- **소액 한글 축약 표기 수정** (`numberFormat.ts` `formatShortMoney`) — 1만원 미만이 "0만"으로 표시되던 것을
  원 단위 그대로 표시하도록 수정, 음수 입력 시 부호가 깨지던 문제도 함께 수정 (버블맵 전일 증감 등 소액 노출 화면 대비)
- **포트폴리오 분석 패널 3개 조회 정리** (`PortfolioAssetClassPanel`/`PortfolioBubblePanel`/`PortfolioComparePanel`) —
  `auth.getUser()`(Auth 서버 왕복) + `portfolios` 직접 조회를 userData 스토어 `ensurePortfolios()` 캐시로 교체
  (2026-07-04 캐싱 컨벤션과 일치, 페이지 전환마다 반복 조회 제거)
- **`stock-price` Edge Function 재배포 완료 확인** — 버블맵 등락률용 `changeRate` 응답 필드 포함 버전이
  Supabase에 배포되어 있음 (사용자가 직접 배포함, 2026-07-13 확인)
- **포트폴리오 분석 패널 시세 캐시 공유** (`market.ts` `getCachedStockQuote`) — 버블/비교 패널이 같은 종목을
  각자 조회하거나 탭을 오갈 때 시세 API가 중복 호출되던 문제 해결. `market.ts`에 60초 TTL 시세 캐시(환율 캐시와
  동일 패턴)를 두고 두 패널이 공유. 비교 패널은 `getStockPrice` → `getCachedStockQuote().price`로 전환. 성공
  응답만 캐시(실패는 재시도), 자산 화면·백그라운드 재계산은 캐시 미사용으로 수동 새로고침 즉시성 유지

---

## ✅ 완료된 작업 (2026-07-12) — 매도 검증 + 가계부 메모 자동완성 + 안드로이드 롱프레스 수정

- 거래 추가/수정 다이얼로그(`TransactionAddDialog.vue`)에 보유수량 초과 매도 검증 추가. 종목 목록 조회 시
  `quantity` 컬럼을 함께 가져와 매도 가능 수량 계산, 수정 모드에서는 원래 거래가 SELL이었을 경우 그 수량을
  되돌려 더한 뒤 비교. 초과 시 안내 문구는 좁은 필드 error 슬롯 대신 전체 폭 배너로 분리(줄바꿈으로 아래
  필드와 겹쳐 보이던 문제 방지), 종목 미선택 상태에서 오표시되던 버그도 수정. DB 트리거 레벨 검증은 이번
  범위에서 제외(프론트만 진행하기로 결정)
- 가계부 내역 추가 다이얼로그(`BudgetEntryAddDialog.vue`) 메모 입력을 `v-combobox`로 교체, 본인이 과거
  입력했던 메모 문자열(카테고리 무관 전체)을 후보로 자동완성. 입력값이 없을 때는 후보를 보여주지 않고
  최소 1글자 입력 시에만 like 검색으로 필터링되도록 처리
- 아이콘 이미지 롱프레스 시 확대/복사/공유/저장 메뉴 방지 — iOS는 기존 `-webkit-touch-callout` CSS로
  막혔지만 안드로이드 Chrome은 이 속성이 적용되지 않고 롱프레스 시 `contextmenu` 이벤트로 메뉴가 뜨는
  구조라 여전히 노출되던 문제 확인, 전역 `contextmenu` 리스너로 `img` 대상 이벤트 차단 추가(`main.ts`)

---

## ✅ 완료된 작업 (2026-07-11) — 다국가 지원 (언어 + 통화)

상세 설계·진행 기록은 **GLOBALIZATION.md** 참고. 1차 목표(한국/미국, ko·en, KRW·USD) 기준으로 자산관리
모듈은 사실상 완료. 프론트의 `asset_type` 참조도 전면 제거 완료(단계 6 준비 끝) — 남은 건 **컬럼 DROP SQL 실행**뿐.
추후 다른 국가/언어/통화 추가 시 손댈 곳은 **ADD_LOCALE.md** 참고.

- 단계 A: `asset_type`(국내주식/해외주식/...) 한글 분기를 `asset_class`+`market` 2축 체계로 전면 교체
- 단계 B: Edge Function(stock-price/etf-info/etf-backtest/etf-dividend) market 기반 시장 서픽스 처리로 개편
- 단계 C: 기준통화(base currency) 도입 — `investment_goals.base_currency` 기반 집계, 원화/달러 토글을 "기준통화 ↔ 미리보기"로 재해석
- 단계 D: `vue-i18n` 도입, 자산관리 전체 화면(메인 탭 4개 + 더보기 하위 화면 전체 + 허브 + 공용 화면) 한글→ko/en i18n 전환
- 단계 E: 티커 표시명 로케일화 — ko는 한글 매핑, 그 외는 티커 원문 폴백
- 보유자산 등록/거래 추가 다이얼로그를 "자산군 + 시장" 조합 선택 UI로 재설계(로케일 따라 기본 시장 자동 선택)
- **정책 확정**: 공지사항·개발자 노트 등 관리자 작성 콘텐츠는 번역하지 않고 한글 원문 유지(자동번역 API는 도입 안 함)
- 화면설정 화면에 화폐(표시통화) 선택 UI 추가, 언어 변경 시 표시통화 자동 전환(ko→KRW, en→USD) + 최초 1회 안내 토스트. 이후 사용자가 화폐만 따로 바꾸면 그 설정 유지. 화면 곳곳에 흩어져 있던 `<CurrencyToggle />` 토글 9곳 전부 제거하고 화면설정으로 일원화(컴포넌트 파일 자체도 삭제)
- 사용자 단계 3·4·5 SQL을 `supabase/migrations/20260711_01~03`으로 기록(이미 실행 완료분). **단계 6 준비**: 프론트의 `asset_type` 참조(insert dual-write 2·select 4·인터페이스 8곳) 전면 제거 + 죽은 전환기 폴백 정리. 컬럼 DROP SQL은 `20260711_04`에 실행 대기로 보관
- ⚠️ 사용자가 할 일: **(단계 6) `20260711_04` DROP SQL은 이 브랜치가 main 배포·안정화된 뒤 실행** (지금 실행하면 구버전 프론트 insert가 깨짐) / Edge Function 4개 Supabase 재배포(아직이면) / 로컬 `npm install`(vue-i18n 신규 의존성) / 실제 로그인 후 자산 등록·거래 추가 플로우 및 English 전환 화면 직접 확인

---

## ✅ 완료된 작업 (2026-07-08) — 이메일 인증 도입

### 배경

- 구글 플레이 출시를 앞두고 보안 점검 중 이메일 인증이 꺼져 있어 아무 형식의 이메일로나 가입이 가능했던 점을 발견, 인증 활성화 결정
- 예전에도 시도했으나 Sender 주소가 `gmail.com`(Resend 미인증 도메인)이라 발송이 막혀서 방치했던 이력 확인 (Resend 로그에서 403 "Testing domain restriction" 원인 확인)

### 이메일 발송 인프라 구축

- Resend 가입, `firepath.me` 도메인 등록 및 GoDaddy 자동 연동으로 DNS(DKIM/SPF) 인증 완료
- Supabase Authentication > SMTP Settings에 Resend 연결 (Host: `smtp.resend.com`), Sender를 `noreply@firepath.me`로 수정
- 노출됐던 기존 API 키/미사용 "Onboarding" 키 정리
- "Confirm sign up" 이메일 템플릿에 로고(`logo-wide-nature.png`) 및 한글 안내 문구 추가
- URL Configuration의 Site URL을 `http://localhost:3000`(방치된 로컬 기본값) → `https://firepath.me`로 수정, Redirect URLs에 `https://firepath.me/**`, `http://localhost:3820/**` 추가 (인증 링크 클릭 시 "네트워크 서버에 연결할 수 없음" 오류 원인이었음)
- Authentication > Sign In/Providers에서 **Confirm email 활성화**

### 소스 수정

- 회원가입 성공 메시지를 "가입 확인 메일을 발송했습니다..."로 변경 (`LoginView.vue`)
- `email_not_confirmed` 에러 메시지를 "이메일 인증이 완료되지 않았습니다"로 명확화 (`errorMessage.ts`)
- 관리자 전용 RPC `admin_get_email_confirmations()` 신규 추가 — `auth.users.email_confirmed_at` 조회 (SECURITY DEFINER, 관리자 이메일만 호출 가능)
- 가입 이력 화면(`AdminSignupLogView.vue`)에 **활성 / 미인증 / 탈퇴** 3단계 상태 표시 추가
- 회원 현황 대시보드(`AdminView.vue`) 활성 회원 집계에서 미인증 계정 제외, 잔존율도 미인증 대기자를 분모에서 제외하도록 수정

---

## 💭 검토했으나 보류한 것 (나중에 다시 꺼내지 않도록 기록)

### 연도별 예상자산 추이 - 지난 연도 표시

- `AnalysisView.vue`의 "연도별 예상자산 추이" 카드는 `year = currentYear`부터만 루프를 도는 순수 예측(projection) 테이블. 템플릿/CSS에 `past` 상태(✓ 완료 뱃지 등) 스타일은 이미 있지만, 로직상 `year < currentYear`가 절대 참이 될 수 없어서 실제로는 사용되지 않는 죽은 코드임.
- "지난 연도도 보여주자"는 아이디어가 나왔었으나 **보류 결정**: 이 카드는 실제 과거 자산 기록을 저장/참조하지 않고 현재 자산 기준 미래 추세만 계산하기 때문에, 지난 연도를 단순히 "완료 100%"로 하드코딩하면 중간에 매도해서 자산이 줄어든 경우에도 항상 "완료"로 표시되어 **실제와 다른 잘못된 정보**를 보여주게 됨.
- 정확하게 하려면 `asset_history`(매일 자정 KST 실제 스냅샷) 데이터를 가져와 "그 시점 목표"와 비교해야 하는데, 목표/월 투자금/기대수익률을 나중에 바꾸면 "그때 계산했던 목표"가 무엇이었는지도 애매해져서 설계가 복잡해짐. 실익 대비 복잡도가 높다고 판단해 현재 방식(과거 연도는 표시하지 않고 그냥 리스트에서 빠짐) 유지하기로 함.

---

## ✅ 완료된 작업 (2026-07-04) — 글자 크기 조절 기능 + 모바일 버그 수정

### 글자 크기 조절 기능 신규 구현

- 더보기 > 화면 설정 메뉴 신설 — 슬라이더로 75%~150%(1% 단위) 조절, 100% 초기화 버튼
- `--font-scale` CSS 변수 + `useFontScale` 컴포저블 도입, 값은 로컬스토리지에 저장
- 프로젝트 전체 하드코딩된 `font-size: Npx`를 `rem`으로 전환 (30여개 파일) — 슬라이더 값 하나로 앱 전체 텍스트가 함께 커지고 작아짐
- 차트(SVG) 안 텍스트는 CSS가 아니라 태그 속성이라 이번 전환 대상에서 제외 (자산성장리포트/예측/FIRE시뮬레이터/ETF백테스트/FIRE히스토리 5개 화면 — 보류)
- 극단적으로 크게 설정했을 때 레이아웃이 깨지던 곳 보강: 자산·거래내역 헤더가 단어 중간에서 줄바꿈되던 문제, 요약 카드 값이 카드 밖으로 넘치던 문제, 종목명이 말줄임 없이 뚝 잘리던 문제
- 자산 요약 카드: 화면에 안 보이는 측정용 사본으로 실제 겹침 여부를 측정해서, 진짜 넘칠 것 같을 때만 매입금액/평가손익/평가금액/수익률 4칸이 동시에 라벨-값 스택 레이아웃으로 전환되도록 구현 (여유 있으면 항상 원래대로 한 줄 유지)

### 코드 정리

- 죽은 Vuetify 타이포그래피 클래스(`text-caption`/`text-body-1`/`text-body-2`/`text-h5`/`text-h6`) 제거 — 31개 파일, 303곳. 설치된 Vuetify 4(Material Design 3)에는 이 옛날 클래스명이 아예 존재하지 않아 원래도 스타일 효과가 없던 죽은 클래스였음 (제거해도 화면상 변화 없음)
- 미사용 디자인 토큰 `src/design/tokens/typography.ts`(fontSize/fontWeight) 삭제 — 정의만 되고 실제로 쓰는 곳이 없었음

### 모바일 pull-to-refresh 버그 수정

- 당겨서 새로고침 시 상단에 배경색 없는 검은 영역이 잠깐 보이던 문제 수정 (네이티브 오버스크롤 바운스가 커스텀 인디케이터와 같이 발생하던 것 — 당기는 순간에만 `preventDefault`로 차단)
- 위 수정 과정에서 생겼던 "화면 스크롤이 전혀 안 되던" 회귀 버그 재수정 (CSS `overscroll-behavior` 전역 차단 대신 제스처 발생 시점에만 조건부로 막는 방식으로 교체)
- 새로고침이 트리거되는 시점에 짧은 진동 피드백 추가 (Android Chrome만 동작 — iOS Safari는 Vibration API 자체를 지원하지 않아 홈 화면 추가해도 동작 안 함)
- 크롬 콘솔에 뜨던 "Ignored attempt to cancel a touchmove event" Intervention 경고 제거

### 스와이프 카드 모서리 비침 버그 재발 수정

- 이전 세션에서 고쳤던 자산·거래내역 스와이프 카드 모서리 비침이 재발 — 분수 배율(fractional device pixel ratio, 예: 크롬 반응형 모드)에서 래퍼와 카드가 각각 독립적으로 계산하는 클리핑 경계가 서브픽셀 단위로 어긋나는 게 근본 원인이었음
- 경계를 정밀하게 맞추려는 시도(반경 여유값, clip-path) 대신, 수정/삭제 버튼 영역 자체를 카드 경계보다 사방으로 6px 작게 배치하는 방식으로 견고하게 재수정 — 서브픽셀 오차가 나도 배경만 보이고 버튼 색은 안 비침

### 예측/자산성장리포트 화면 개선

- "연도별 예상자산 추이"에서 올해 목표 표시를 퍼센트 대신 다른 연도와 동일하게 금액으로 변경, 서브라벨 가독성 개선
- 자산성장리포트 차트의 "올해 목표"/"전체 자산" 라벨에 금액을 함께 표시, "전체 자산"→"현재 자산"으로 통일
- "막대 클릭 시 일별 보기" 안내 문구를 "👆 막대를 눌러 일별 증감 보기"로 개선
- 자산 요약 카드 "수익률(%)" 라벨을 "수익률"로 축약 (값 자체에 이미 %가 표시되어 중복)

---

## ✅ 완료된 작업 (2026-07-04) — 화면 간 데이터 캐싱 + 사용성 개선

### 공유 데이터 스토어 도입

- Pinia 스토어(`src/stores/userData.ts`) 신설 — `investment_goals`/`asset_summary`/`portfolios`를 화면 간 캐싱해 탭 전환 시마다 반복되던 API 호출 제거
- 로그아웃/회원탈퇴 시 스토어 초기화 연결 (계정 전환 시 이전 데이터 노출 방지)
- 대시보드·자산·거래내역·예측·FIRE 시뮬레이터·FIRE 진행기록·목표 달성 배지·자산 성장 리포트·목표 설정 화면에 스토어 연결
- 목표 저장/보유종목 변경/거래 추가·수정·삭제 시 캐시 무효화 로직 추가해 정합성 보장
- 평가금액/원가 계산 공식을 `src/utils/portfolioMath.ts`로 추출해 자산 화면과 백그라운드 재계산 로직이 공용으로 사용하도록 정리

### 버그 수정

- 거래 추가/수정/삭제 직후 대시보드 총자산이 반영되지 않던 문제 수정 (백그라운드 재계산 함수 신설)
- 거래내역 화면 진입 시 월 옵션이 중복 조회되던 버그 수정
- 아래로 당겨서 새로고침 도입 과정에서 생긴 "화면 스크롤이 전혀 안 되던" 버그 수정
- 자산·거래내역 화면 스와이프 카드 모서리에 수정/삭제 버튼 색이 비치던 렌더링 버그 수정

### 네트워크 호출 최적화

- 화면 곳곳의 `auth.getUser()`(Auth 서버 왕복 발생) → `auth.getSession()`(로컬 조회, 네트워크 호출 없음) 교체
- 더보기 화면에서 중복 조회되던 미확인 피드백 개수를 AppLayout과 공유하도록 변경

### 사용성 개선

- 메인 탭(홈/자산/기록/예측)에 아래로 당겨서 새로고침 기능 추가
- 자산 화면 상단 새로고침 버튼 제거 (당겨서 새로고침으로 대체되어 중복)
- 스와이프 카드 — 열린 카드 외 아무 영역이나 클릭해도 닫히도록 개선 (기존엔 다른 카드를 눌러야만 닫힘)
- 자산 화면 카드 폰트 크기·굵기, 하단 안내문구·여백을 거래내역 화면과 통일

---

## ✅ 완료된 작업 (2026-07-01) — 현금 자산 관리 + 공지사항 기능

### 현금 자산 관리

- 보유자산 화면 자산유형에 "현금" 다시 추가 (원화/달러 개별 관리, 계좌 구분 지원)
- 현금 카드에 계좌 구분 배지 누락 수정
- 현금 표시명 간소화 (원화현금→원화, 달러현금→달러)
- 대시보드 현재 자산 카드에 "투자자산만/현금 포함" 토글 추가 (FIRE 달성률·예상 달성일·미래예측은 기존처럼 투자자산 기준 유지)
- `investment_goals.include_cash` 컬럼 추가 (토글 상태 기록용, 읽기는 로컬스토리지 우선)

### 관리자 페이지 개선

- 회원 현황: "거래내역 있음" 카드 → "현금 포함 사용" 카드로 교체
- 회원 상세 다이얼로그에 "현금 자산" 항목 추가
- 이력 조회: 페이지 필터 목록을 라우터 `meta.label` 기반 자동 생성으로 변경 (신규 메뉴 추가 시 코드 수정 없이 자동 반영)
- 이력 조회: "테스트 계정 제외" 토글 추가 (로컬스토리지 저장, iOS 스타일 스위치)
- 이력 조회: "세션 이력" 탭 신규 추가 (access_log를 1시간 공백 기준 세션 단위로 그룹핑해 "오늘 접속 여부" 확인 가능)
- 이력 조회: 탭 순서 조정 (세션 이력을 페이지 접속 이력보다 앞으로)

### ETF 백테스트

- ETF 비교 카드 MDD(최대낙폭) 승리 판정 로직 버그 수정 — 낙폭이 더 큰 쪽이 잘못 승리로 표시되던 문제

### 공지사항 신규 기능

- `notices` 테이블 신설 (관리자만 작성 가능, 로그인 유저 전체 조회 가능, RLS 적용)
- 관리자 전용 공지 작성/수정/삭제 화면 (`/admin/notices`)
- 더보기 > 서비스에 "공지사항" 메뉴 추가, 목록 조회 화면 (`/notices`)
- 대시보드 접속 시 신규 공지 최초 1회 팝업 노출 (유저 단위로 로컬스토리지 추적, 기기 공유 시에도 계정별 독립 판단)
- "테스트 공지" 모드 추가 — 체크 시 RLS로 필터링되어 관리자만 확인 가능, 실제 사용자에게는 노출 안 됨

---

## ✅ 완료된 작업 (2026-06-30) — 계좌 구분 UI 개선

### 자산 화면 개선

- 자산 카드 3줄 → 2줄 레이아웃 (수량·평가금액·손익 한 줄)
- 종목명·티커·계좌 배지 한 줄 인라인 flex 정렬
- 국내주식 종목코드 숨김 (해외주식만 티커 표시)
- 계좌명 truncate: 한글 2자 / 영문 4자
- 자산 카드 크기 축소 (pa-3→pa-2, 로고 28px→22px, 폰트 축소)

### 자산 추가/수정 다이얼로그 개선

- density="compact"로 전환, 불필요한 공백 제거
- 계좌 구분 빈값 blur 시 자동으로 '미지정' 세팅
- 수정 모드: 섹션명 "초기 잔고" / 안내 문구 개선
- 수정 모드: 현재 총 보유수량 읽기전용 표시
- 수정 모드: 계좌명 변경 시 중복 체크 추가
- "초기 잔고" → "현재 보유 내역" 텍스트 개선

### 거래 추가 다이얼로그 개선

- density="compact"로 전환, 공백 최소화
- 계좌 필터 변경 시 직접 입력한 계좌명 덮어쓰기 버그 수정

### 거래내역 화면 버그 수정

- 무한스크롤 오작동 수정 (hasMore 판단을 현금 필터링 후 기준으로 변경)

### 더보기 개선

- 개발자 노트 메뉴 및 페이지 추가 (v1.0.0, v1.0.1 업데이트 내역)

---

## ✅ 완료된 작업 (2026-06-29) — ETF 백테스트

### ETF 백테스트 기능 신규 구현

- Supabase Edge Function `etf-backtest` 신규 작성 (Yahoo Finance adjclose 기반 월별 DCA 시뮬레이션)
- 결과 지표: 최종 평가금액, 총 수익금, 총 수익률, CAGR, MDD(날짜 포함), 투자기간, 투자원금, 최고 평가금액
- SVG 누적 자산 추이 차트 (터치/마우스 인터랙션 툴팁)
- 차트 금액/수익률(%) 모드 토글 (비교 시 스케일 차이 문제 해결)
- ETF 비교 기능: 두 티커 병렬 조회, 비교 테이블(왕관 아이콘), 차트 2개 라인
- 자연어 요약 카드
- 연도별 스냅샷 테이블 (최근 5년 기본 / 전체 보기 토글)
- 최근 조회 ETF 칩 (localStorage 최대 5개, 기준/비교 인풋 포커스 추적)
- 시작일 빠른 선택 버튼 (1년/3년/5년/10년/전체) + 연도/월 2단계 직접 선택
- DCA·CAGR·MDD·총수익률 인라인 ⓘ 툴팁 (모바일 클릭 방식)
- 존재하지 않는 티커 오류 처리 (Edge Function 200 응답으로 통일)
- 해외 전용 안내 문구, 수익률 음수 케이스 차트 Y축 대응
- MoreView 메뉴 활성화, 라우터 등록.

---

## ✅ 완료된 작업 (2026-06-27) — 세션 2

### 미래예측 차트 전면 개선

- projection을 월 단위 → **일 단위**로 전환 (더 정밀한 예측)
- 차트 Y축을 **로그 스케일**로 변경 → 과거/미래 성장 비례 표현
- 과거 실선(asset_history) + 미래 예측 점선 구분 렌더링
- 과거·예측 연결 끊김 현상 수정 (마지막 과거 포인트를 점선 시작점에 포함)
- 마일스톤 step 동적 설정 (목표 금액에 따라 1억/5억/10억/50억 단위, 최대 4개)

### 연도별 예상자산 추이 정확도 개선

- 예상 자산을 연초 기준 → **12월 말 기준** 월 단위 계산으로 변경
- 단기 목표(3년 미만) 예외처리 → 전체 FIRE 달성률만 표시
- `yearlyRows`를 projection 의존에서 독립 계산으로 변경

### FIRE 목표 달성 카드 개선

- 예상 자산: 연도 말 값 → **목표 달성 월의 자산**(목표치 근접)으로 변경
- 날짜 표시: "2027년 예상" → **"2027년 9월 예상"** (월 단위)

### asset_history 테이블 연동

- pg_cron `daily-asset-snapshot` (매일 KST 00:00) 스케줄 확인 및 CLAUDE.md 문서화
- PortfolioView 중복 upsert 제거 (pg_cron으로 대체)
- AnalysisView에서 asset_history 로드 → 차트 과거 실선에 활용

### CLAUDE.md 문서화

- asset_history, login_log, signup_log, feedback 테이블 스키마 추가
- pg_cron 스케줄 및 save_daily_asset_snapshot 함수 내용 기록

---

## ✅ 완료된 작업 (2026-06-27) — 세션 1

### 사용자 의견 보내기 시스템

- 더보기 메뉴 "의견 보내기" 항목 문구 업데이트
- 의견 보내기 폼: 구분(버그신고/기능제안/기타의견)/제목/내용 입력, Supabase `feedback` 테이블 저장
- 전송 완료 UX: 다이얼로그 → 전체 페이지 전환 방식으로 개선
- AdminFeedbackView 신규: 피드백 목록 카드 UI, 카테고리 필터/검색, 인라인 상태 변경(미확인→확인→완료), 삭제
- AdminView: 미확인 의견 N건 뱃지 + 사용자 의견 메뉴 추가
- 라우터: `/admin/feedback` 경로 추가
- 스낵바 노출 시간 3초 → 2초 변경

### 국내 주식 티커 한글명 목록 업데이트

- `tickerNames.kr.ts` 대규모 갱신

---

## ✅ 완료된 작업 (2026-06-26)

### 거래내역 화면 개선

- 무한스크롤 구현 (30건 페이지네이션, IntersectionObserver)
- stat 카드 USD+KRW 환율 변환 합산, 적용환율 표시 (전일 기준)
- 카드 UI 개선: 한글 종목명, BUY=+초록/SELL=-빨강, 메모 툴팁 아이콘
- 월 헤더 제거
- 날짜 필터: 연/월 드롭다운 (실거래 기간만 표시)
- stat 카드 필터/날짜 조건 연동
- 삭제 시 스크롤 위치 유지, 수정 후 스크롤 복원
- 총 건수 표시
- 검색 결과 없을 때 문구 분기 처리
- 거래 있는 기간만 조회됩니다 안내 문구

### 종목 등록 검증

- 국내주식/암호화폐: TICKER_NAMES 목록 기반 차단
- 해외주식: stock-price API 실시간 검증
- 거래 추가 다이얼로그 종목 선택에 한글명 표시

### 거래 추가 다이얼로그 개선

- 종목 선택 목록 한글명 표시, 자산유형 "주식" 제거
- 매수/매도 토글 버튼 가독성 개선

### 기타

- Vue DevTools 플로팅 버튼 제거
- 전역 모바일 텍스트 선택 비활성화 (input/textarea 제외)
- 어드민 미국 주식 티커 CRUD 페이지 완성
